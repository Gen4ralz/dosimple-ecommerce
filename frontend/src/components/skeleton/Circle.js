import Animate from './Animate';

const Circle = () => {
  return (
    <div className="w-10 rounded-lg h-10 bg-indigo-50 overflow-hidden relative">
      <Animate />
    </div>
  );
};

export default Circle;
